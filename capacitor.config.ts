import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vindk.concretemixpro',
  appName: 'ConcreteMix Pro',
  webDir: 'out'
};

export default config;
